<%= form_with(model: [ @snippet, @snippet.comments.build ], local: true) do |form| %>
  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content %>
  </div>
  <div class="actions">
    <%= form.submit 'Aggiungi Commento' %>
  </div>
<% end %>
